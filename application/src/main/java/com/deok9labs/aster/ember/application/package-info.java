/**
 * Ember 유스케이스와 계층 간 계약을 정의한다.
 *
 * <p>Inbound adapter는 이 패키지의 inbound port와 command만 호출한다. Outbound adapter는 outbound port의
 * data와 command를 자신의 기술 객체로 변환하며 Web DTO와 JPA entity는 이 경계를 통과할 수 없다.</p>
 */
package com.deok9labs.aster.ember.application;
